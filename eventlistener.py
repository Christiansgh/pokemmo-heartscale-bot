from utils import *

def await_fish_or_opponent_ready():
    while True:
        # find fish textbubble
        # set state to fishing
            return
        # find battlescreen
        # set state to battling
            return

def await_move_ready():
    while True:
        # find move ready 
            return

def await_heal_ready():
    while True:
        # find heal ready
            return

def await_opponent_found():
    while True:
        # find opponent
            return
